name: Deployement Workflow

# --- Étape 3 : Conditions de lancement ---
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

# --- Étape 4 : Définition du job ---
jobs:
  deploy:                   # C'est le nom de ton Job
    runs-on: ubuntu-latest  # C'est ton Runner (une machine virtuelle Linux)
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
