name: CI-CD Deployment

on:
  push:
    branches: [ "principal" ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Test
        run: echo "Déploiement prêt !"
